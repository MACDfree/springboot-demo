<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.macd.springbootdemo.dao.ArticleDao">
    <select id="queryArticle" resultType="me.macd.springbootdemo.pojo.Article">
        select * from article
    </select>
    <select id="queryArticleById" resultType="me.macd.springbootdemo.pojo.Article">
        select * from article where id = #{id}
    </select>
    <insert id="insertArticle" useGeneratedKeys="true" keyProperty="id" keyColumn="id"
            parameterType="me.macd.springbootdemo.pojo.Article">
        insert into
        article(content,create_time,update_time,delete_status)
        values
        (#{content},#{createTime},#{updateTime},#{deleteStatus})
    </insert>
    <update id="updateArticle" parameterType="me.macd.springbootdemo.pojo.Article">
        update article
        <set>
            <if test="content!=null">content=#{content},</if>
            <if test="updateTime!=null">update_time=#{updateTime},</if>
            <if test="deleteStatus!=null">delete_status=#{deleteStatus}</if>
        </set>
        where id=#{id}
    </update>
    <delete id="deleteArticle">
        delete from article
        where id=#{id}
    </delete>
</mapper>